[INSERT]
default = """
INSERT INTO metric.cpu (
    collected_at, server_id, cpu_percent, cpu_count
) VALUES (
    %(collected_at)s, %(server_id)s, %(cpu_percent)s, %(cpu_count)s
)
ON CONFLICT (collected_at, server_id) DO NOTHING
"""
